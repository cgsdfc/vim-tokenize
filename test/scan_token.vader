Execute(NL);
call tokenize#helper#ScanLine('           ')
call tokenize#helper#ScanLine("           \n")

Execute(Number);
call tokenize#helper#ScanLine(' 0b01_01 .1 2.1 3.e4 0x12 '."\n")

Execute(Name);
call tokenize#helper#ScanLine(' name name name '."\n")

Execute(Operator);
call tokenize#helper#ScanLine(' >>= @ @= += - * '."\n")

Execute(Bracket);
call tokenize#helper#ScanLine(' [ { ( ) } ] '."\n")

Execute(Special);
call tokenize#helper#ScanLine(' ; : ... . , @'."\n")
call tokenize#helper#ScanLine("   \n")

Execute(Error FIXME);
call tokenize#helper#ScanLine(' $ ? ` ')

Execute(ContStr);
call tokenize#helper#ScanLine(' "string"                    ')
call tokenize#helper#ScanLine(' ''string''         ')
call tokenize#helper#ScanLine(" \"string\\\n")

Execute(PseudoExtras);
call tokenize#helper#ScanLine('  # comment'."\n")
call tokenize#helper#ScanLine('b"""'."\n")
