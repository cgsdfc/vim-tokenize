Execute(NL);
call tokenize#line_scanner#ScanLine('           ')
call tokenize#line_scanner#ScanLine("           \n")

Execute(Number);
call tokenize#line_scanner#ScanLine(' 0b01_01 .1 2.1 3.e4 0x12 '."\n")

Execute(Name);
call tokenize#line_scanner#ScanLine(' name name name '."\n")

Execute(Operator);
call tokenize#line_scanner#ScanLine(' >>= @ @= += - * '."\n")

Execute(Bracket);
call tokenize#line_scanner#ScanLine(' [ { ( ) } ] '."\n")

Execute(Special);
call tokenize#line_scanner#ScanLine(' ; : ... . , @'."\n")
call tokenize#line_scanner#ScanLine("   \n")

Execute(Error FIXME);
call tokenize#line_scanner#ScanLine(' $ ? ` ')

Execute(ContStr);
call tokenize#line_scanner#ScanLine(' "string"                    ')
call tokenize#line_scanner#ScanLine(' ''string''         ')
call tokenize#line_scanner#ScanLine(" \"string\\\n")

Execute(PseudoExtras);
call tokenize#line_scanner#ScanLine('  # comment'."\n")
call tokenize#line_scanner#ScanLine('b"""'."\n")
